# document-to-markdown

A small local converter for PDFs, Word, Excel, and PowerPoint files using Microsoft's [MarkItDown](https://github.com/microsoft/markitdown).

## setup and use

```bash
cd /Users/anx/document-to-markdown
./setup.sh
./convert.py
```

Place files in `sources/`. Markdown output goes to `markdown/`.
